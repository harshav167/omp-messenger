services:
  mesh:
    build:
      context: ..
      dockerfile: mesh/Dockerfile
    ports:
      - "8765:8765"
    environment:
      PI_MESSENGER_MESH_TOKEN: ${PI_MESSENGER_MESH_TOKEN:?set PI_MESSENGER_MESH_TOKEN}
    volumes:
      - mesh-data:/data
    restart: unless-stopped
volumes:
  mesh-data: {}
